{% extends 'front.html.twig' %}

{% block title %}Suivi{% endblock %}

{% block body %}
    <h1>Suivi</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Date</th>
                <td>{{ suivi.date ? suivi.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ suivi.poids }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ suivi.taille }}</td>
            </tr>
            <tr>
                <th>HeureActivite</th>
                <td>{{ suivi.heureActivite }}</td>
            </tr>
            <tr>
                <th>ConsoEau</th>
                <td>{{ suivi.consoEau }}</td>
            </tr>
            <tr>
                <th>Idutilisateur</th>
                <td>{{ suivi.idutilisateur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('suivi_index') }}">back to list</a>

    <a href="{{ path('suivi_edit', {'id': suivi.id}) }}">edit</a>

    {{ include('suivi/_delete_form.html.twig') }}
{% endblock %}
